services:

  nginx:
    image: nginx:alpine
    container_name: nginx
    restart: always
    ports:
      - 80:80
      - 443:443
    volumes:
      - /var/www/fleyt.ru:/var/www/fleyt.ru
      - ./nginx:/etc/nginx/conf.d
